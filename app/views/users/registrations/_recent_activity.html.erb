<% case activity.class.to_s
   when 'Comment' %>
  Commento a <%= link_to activity.commentable_root.title, activity.commentable_root %>: <i>"<%= sanitize(activity.body).truncate(25) %>"</i>
<% when 'Post' %>
  Post sul blog: <%= link_to activity.title, activity %>
<% end %>

 (<%= activity.activity_date.strftime("%d/%m/%Y") %>)</br>
